package com.covid.nodetrace.database

import android.content.Context
import com.covid.nodetrace.Contact

class FirebaseDatabase () : FirebaseDao {

    override suspend fun create(context: Context, contact: Contact) {
        TODO("Not yet implemented")
    }

    override suspend fun read(context: Context, contact: Contact) {
        TODO("Not yet implemented")
    }

    override suspend fun update(context: Context, contact: Contact) {
        TODO("Not yet implemented")
    }

    override suspend fun delete(context: Context, contact: Contact) {
        TODO("Not yet implemented")
    }
}